function personInfo(firstName, lastName, age) {
    let person = { firstName,
        lastName,
        age
        };

    return person;
}

console.log(personInfo('peter', 'petrov', '19'));

personInfo("Peter", 
"Pan",
"20")
